import { SharkDirective } from './shark.directive';

describe('SharkDirective', () => {
  it('should create an instance', () => {
    const directive = new SharkDirective();
    expect(directive).toBeTruthy();
  });
});
